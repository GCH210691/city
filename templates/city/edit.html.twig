{% extends "base.html.twig" %}

{% block title %}Edit City
{% endblock %}

{% block body %}
	<div class="container col-md-6 text-center mt-3 card p-2 bg-warning">
		<h1 class="text text-danger">Edit city</h1>
		{{ form_start (CityForm) }}

		{{ form_widget (CityForm) }}
		<input class="btn btn-primary" type="submit" value="Edit">
		{{ form_end (CityForm) }}
	</div>
{% endblock %}
